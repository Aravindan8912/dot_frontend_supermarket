/**
 * Shared dialog component for the app.
 * Use <app-dialog> with [isOpen], [title], (closed), and optionally [maxWidth].
 * Project content into the body and use [dialogActions] for footer buttons.
 *
 * Import: import { DialogComponent } from '../../../shared/components/dialog';
 */
export { DialogComponent } from './dialog.component';
